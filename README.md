# bloom-docker-compose
TODO:
SWAG
Also need to enter the credentials into the corresponding ini (or json for some plugins) file under /config/dns-conf.
set staging to 
https://hub.docker.com/r/linuxserver/swag
